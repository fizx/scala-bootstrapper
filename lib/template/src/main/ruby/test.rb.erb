#!/usr/bin/ruby

require "rubygems"
require "thrift_client"
$:.push('./target/gen-rb')
require "birdname_service"

client = ThriftClient.new(BirdName::BirdNameService::Client, "localhost:9999", :transport_wrapper => Thrift::FramedTransport)
p client.get_name()
